#!/bin/bash

name=chromedriver
url=http://chromedriver.storage.googleapis.com/2.12/chromedriver_linux64.zip

[ ! -d bin ] && mkdir bin
if [ ! -f bin/$name ]; then
    wget $url -O bin/$name.zip
    unzip bin/$name.zip -d bin
    rm bin/$name.zip
fi

./bin/$name
